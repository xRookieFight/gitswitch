/target
**/*.rs.bk

# Intermediate render data for the README images; the PNGs and the GIF are
# committed, the JSON frames are regenerated by `cargo run --example screenshots`.
/docs/frames

# Local editor and OS noise
.idea/
.vscode/
.DS_Store
